import 'package:flutter/material.dart';

class AppColors {
  // Piramit Logo Yeni Palet
  static const Color darkGrey = Color(
    0xFF333333,
  ); // En sağdaki koyu füme (Metinler)
  static const Color lightGrey = Color(
    0xFFE0E0E0,
  ); // En soldaki açık gri (Kenarlıklar)
  static const Color background = Color(
    0xFFF5F5F5,
  ); // Yumuşak beyaz/gri arka plan

  // Yeşil Tonları (Vurgu Renkleri)
  static const Color primaryGreen = Color(
    0xFF66D2B3,
  ); // Piramitteki canlı su yeşili (Butonlar)
  static const Color lightGreen = Color(
    0xFFB2EBE0,
  ); // Piramitteki açık mint (Hafif vurgular)
  static const Color deepGreen = Color(
    0xFF4DB69E,
  ); // Bir tık koyu yeşil (Hover/Aktif durumlar)

  static const Color white = Colors.white;
}
